<hgroup class="title">
    <h3>@ViewBag.Title.</h3>
    @{ if (!string.IsNullOrEmpty((string)ViewBag.Message)) {<h4 class="message-error">@ViewBag.Message</h4>}}
</hgroup>



<h5 class="error message-error">Revise, no se puede mostrar su pedido porque la configuración mínima de parámetros no esta completa.</h5>